{
    "builds": [{
        "src": "smartreport/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "25mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "smartreport/wsgi.py"
        }
    ]
}